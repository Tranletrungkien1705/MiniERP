services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__Default=Data Source=/data/minierp.db
    volumes:
      - minierp-data:/data

volumes:
  minierp-data:
